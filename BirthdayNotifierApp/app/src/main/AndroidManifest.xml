<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.birthdaynotifier">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:label="Birthday Notifier"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Birthday list editor UI -->
        <activity android:name=".presentation.BirthdayListActivity" />

        <!-- Main screen after login -->
        <activity android:name=".presentation.MainActivity" />

        <!-- Login screen -->
        <activity android:name=".presentation.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Receiver for scheduled birthday notifications -->
        <receiver android:name=".framework.receiver.BirthdayReceiver"
            android:exported="false" />
    </application>

</manifest>