<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jlianes.birthdaynotifier">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_cake_main"
        android:roundIcon="@drawable/ic_cake_main"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BirthdayNotifier">

        <!-- Birthday list editor UI -->
        <activity android:name=".presentation.BirthdayListActivity" />

        <!-- Settings screen -->
        <activity
            android:name=".presentation.SettingsActivity"
            android:configChanges="locale|layoutDirection|uiMode|screenSize" />

        <!-- Login screen -->
        <activity android:name=".presentation.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Receiver for scheduled birthday notifications -->
        <receiver android:name=".framework.receiver.BirthdayReceiver"
            android:exported="false" />

        <!-- Receiver for snoozing notifications -->
        <receiver android:name=".framework.receiver.SnoozeReceiver"
            android:exported="false" />

        <!-- Receiver to resend notifications after snooze -->
        <receiver android:name=".framework.receiver.SnoozedNotificationReceiver"
            android:exported="false" />

        <!-- Reschedule alarm after device reboot -->
        <receiver android:name=".framework.receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>